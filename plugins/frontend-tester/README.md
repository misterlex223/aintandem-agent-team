# Frontend Tester Plugin

AI 驅動的前端測試工程師 Plugin，根據產品規格 (PRD) 自動生成測試計劃並執行驗收測試。

## 功能特色

- **PRD 驅動測試計劃**: 根據 PRD.md 自動生成測試計劃和可執行規格
- **規格驅動測試**: 根據 Markdown 格式的產品規格執行測試
- **雙層測試模式**: 功能驗收 + 完整版本驗收
- **混合測試模式**: 支援自動化測試和人工檢查
- **Playwright 整合**: 使用 Playwright MCP 執行瀏覽器自動化
- **API 測試**: 支援 REST API 端點驗證
- **測試報告**: 生成結構化的 Markdown 測試報告
- **與 version-manager 整合**: 作為發布流程的一部分

## 安裝

此 Plugin 已內建於 Chromium AI Sandbox，無需額外安裝。

容器啟動時會自動安裝到：
```
/home/flexy/.claude/plugins/frontend-tester
```

### 安裝流程

1. **Dockerfile**: 複製 plugin 到 `/tmp/frontend-tester-plugin`
2. **init-chromium-sandbox.sh**: 安裝到 `/home/flexy/.claude/plugins/`

## 使用方式

### 1. 生成測試計劃

根據 PRD 自動生成測試計劃：

```bash
# 功能驗收模式（針對未完成功能）
/generate-test-plan --mode=feature

# 完整測試模式（整個版本）
/generate-test-plan --mode=full --version=0.7.0

# 指定功能區塊
/generate-test-plan --mode=feature --target=2.8
```

### 2. 執行驗收測試

根據測試計劃執行驗收：

```bash
# 功能驗收
/run-acceptance --mode=feature --target=2.8

# 完整驗收
/run-acceptance --mode=full

# 直接指定規格文件
/run-acceptance --spec-path=docs/specs/login-spec.md
```

### 3. 手動建立規格文件

使用範本建立規格文件：

```bash
/test-spec-template docs/specs/login-spec.md
```

### 4. 查看報告

測試完成後，報告會生成在：

```
docs/test-reports/test-report-[YYYY-MM-DD]-[feature-name].md
```

## 規格文件格式

規格文件使用 Markdown 格式，包含以下區塊：

```markdown
# 功能名稱

## 基本信息
- 功能名稱: [名稱]
- 測試 URL: [URL]
- 測試環境: [開發 | 預產 | 產品]

## 測試場景

### 場景 1: 正常登入
**類型**: 自動化

| 步驟 | 動作 | 預期結果 |
|------|------|---------|
| 1 | 導航到登入頁 | 頁面正常顯示 |
| 2 | 輸入有效帳號密碼 | 表單接受輸入 |
| 3 | 點擊提交 | 登入成功 |

## UI/UX 驗收標準
- [ ] 符合設計稿
- [ ] 響應式布局正常
- [ ] 可訪問性符合 WCAG AA
```

完整範本請參考 `templates/spec-template.md`。

## Plugin 結構

```
plugins/frontend-tester/
├── .claude-plugin/
│   └── plugin.json              # Plugin 清單
├── agents/
│   ├── frontend-test-engineer.md  # 測試工程師 Agent
│   └── acceptance-tester.md       # 驗收測試 Agent (🆕)
├── commands/
│   ├── test-spec.md              # 執行測試命令
│   ├── test-spec-template.md     # 產生規格範本命令
│   ├── generate-test-plan.md     # 生成測試計劃 (🆕)
│   └── run-acceptance.md         # 執行驗收測試 (🆕)
├── templates/
│   ├── spec-template.md          # 測試規格範本
│   └── test-plan-template.md     # 測試計劃範本 (🆕)
├── skills/
│   └── frontend-testing/
│       └── SKILL.md              # 測試技能說明
└── README.md
```

## 可用功能

| 類型 | 名稱 | 說明 |
|------|------|------|
| **Agent** | `frontend-test-engineer` | 自動觸發測試工作流程 |
| **Agent** | `acceptance-tester` | PRD 驅動的驗收測試 Agent (🆕) |
| **Command** | `/generate-test-plan` | 根據 PRD 生成測試計劃 (🆕) |
| **Command** | `/run-acceptance` | 執行驗收測試 (🆕) |
| **Command** | `/test-spec <file>` | 根據規格執行測試 |
| **Command** | `/test-spec-template [path]` | 產生規格範本 |
| **Skill** | `frontend-testing` | 測試最佳實踐 |

## 測試流程

```
┌─────────────┐
│ 1. 解析 PRD │ ← 🆕 PRD 驅動
└──────┬──────┘
       ▼
┌─────────────┐
│ 2. 生成計劃 │ ← 🆕 雙層輸出（計劃 + 規格）
└──────┬──────┘
       ▼
┌─────────────┐
│ 3. 執行測試 │ ← Playwright + API 測試
└──────┬──────┘
       ▼
┌─────────────┐     ┌─────────────┐
│ 3a. 自動化  │     │ 3b. 人工    │
│    測試     │     │    檢查     │
└──────┬──────┘     └──────┬──────┘
       └─────┬────────────┘
             ▼
      ┌─────────────┐
      │ 4. 生成報告 │
      └─────────────┘
```

## 與 version-manager 整合

在發布流程中，frontend-tester 提供驗收測試檢查點：

```markdown
### version-manager 發布檢查清單

### 測試驗證
- [ ] 執行 /run-acceptance --mode=full
- [ ] 測試報告無 critical 失敗
- [ ] 所有 console 錯誤已修復
```

## 輸出檔案

| 檔案 | 說明 |
|------|------|
| `docs/test-plans/TP-[version]-[mode]-[date].md` | 測試計劃（策略層級） |
| `docs/specs/[feature]-spec.md` | 可執行測試規格 |
| `docs/test-reports/test-report-[date].md` | 測試報告 |

## 環境需求

- Chromium AI Sandbox
- Claude Code with MCP support
- Playwright MCP Server (`@playwright/mcp`)
- Node.js 18+

## 授權

MIT License
